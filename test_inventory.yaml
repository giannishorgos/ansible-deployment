dockerservers:
    hosts:
        localhost:
            ansible_port: 2222
            ansible_user: ansible_user
            ansible_become_pass: 1234
            ansible_ssh_private_key_file: ansible_priv
